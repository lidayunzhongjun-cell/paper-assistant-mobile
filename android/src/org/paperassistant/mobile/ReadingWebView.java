package org.paperassistant.mobile;
import android.content.Context;
import android.webkit.WebView;
/** The platform copy/select menu stays unchanged; reading actions are in the bottom toolbar. */
final class ReadingWebView extends WebView {
  ReadingWebView(Context context) { super(context); }
}
