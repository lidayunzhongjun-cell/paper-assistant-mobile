export const mathPrompt = String.raw` 数学符号使用 LaTeX：行内用 \(...\)，独立公式用 \[...\]，保留上下标、分式、求和和原文编号。JSON 字符串中的反斜杠必须转义。只转写能从原文确定的公式；提取缺失时明确说明，不补造公式。`;

export const emphasisPrompt = ' 重点排版要克制：仅对最关键的结论、适用条件或限制使用 **短语** 加粗，每段最多一处，全文强调文字不超过约 15%；普通段落可以不加粗。不加彩色符号、感叹号或整段加粗，不在公式内部插入 Markdown 标记。';
